@model IEnumerable<LVH_ASPNET_Lesson06.Models.LVHSong>

@{
    ViewBag.Title = "LVHSong";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Thêm mới", "lvhCreate")
</p>
<table class="table table-bordered">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.lvhTitle)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.lvhAuthor)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.lvhArtist)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.lvhYearRelease)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.lvhTitle)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.lvhAuthor)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.lvhArtist)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.lvhYearRelease)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
